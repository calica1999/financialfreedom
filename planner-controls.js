(()=>{'use strict';const $=id=>document.getElementById(id);for(const id of ['frequency','needsPct','wantsPct','savingsPct']){const old=$(id);if(!old)continue;const el=old.cloneNode(true);old.replaceWith(el);el.addEventListener('input',()=>{if(id!=='frequency'){const custom=document.querySelector('input[name="rule"][value="custom"]');if(custom)custom.checked=true}const st=$('saveState');if(st){st.textContent='Unsaved changes';st.className='state'}const save=$('save');if(save)save.disabled=false});el.addEventListener('change',()=>el.dispatchEvent(new Event('input',{bubbles:true})))} })();